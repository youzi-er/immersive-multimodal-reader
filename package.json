{
  "name": "immersive-multimodal-reader",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "verify:deploy-payload": "node backend/scripts/verify-deploy-payload.js",
    "build": "npm run verify:deploy-payload && npm run build --workspace frontend",
    "start": "npm run start --workspace backend"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

